name: Sync from production

# Bring production's commits back onto staging `main` by fast-forwarding.
# This keeps staging and production identical and linear so the next `stlc build`
# reseals against the released state.
#
# Polled from private staging so production never needs credentials for private repos.
on:
  schedule:
    # GitHub schedules are best-effort. Manual dispatch remains available for
    # an immediate post-release back-sync.
    - cron: "4,14,24,34,44,54 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: stlc-sync-from-production
  cancel-in-progress: true

jobs:
  sync:
    runs-on: namespace-profile-ci-1x2
    if: github.repository == 'boltz-bio/boltz-api-go-staging'
    env:
      PRODUCTION_REPO: boltz-bio/boltz-api-go
    steps:
      - name: Mint sync token
        id: sync-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3
        with:
          app-id: ${{ secrets.PROMOTE_APP_ID }}
          private-key: ${{ secrets.PROMOTE_APP_PRIVATE_KEY }}
          owner: boltz-bio
          repositories: |
            boltz-api-go-staging
            boltz-api-go
          permission-contents: write

      - name: Check out staging
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Fetch production main
        env:
          SYNC_TOKEN: ${{ steps.sync-token.outputs.token }}
        run: |
          git remote set-url origin "https://x-access-token:${SYNC_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git remote add production "https://x-access-token:${SYNC_TOKEN}@github.com/${PRODUCTION_REPO}.git"
          git fetch production main

      - name: Check whether production has commits staging lacks
        id: diff
        run: |
          # A promotion merge can leave production with the same tree as
          # staging but a newer commit. Preserve that lineage by syncing until
          # production/main is an ancestor of staging/main.
          if git merge-base --is-ancestor production/main origin/main; then
            echo "Staging already contains production's commit. Nothing to pull back."
            echo "behind=false" >> "$GITHUB_OUTPUT"
          else
            echo "behind=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Sync production to staging (fast-forward)
        if: steps.diff.outputs.behind == 'true'
        run: |
          # Refuse unless staging/main is an ancestor of production/main. If it
          # isn't, the trunks have forked (staging advanced out of band while a
          # production change was unsynced) and a fast-forward is unsafe.
          #
          # The trunk-sync lock (trunk-sync-lock.yml) is what normally
          # prevents this by freezing staging merges until the back-sync lands.
          if ! git merge-base --is-ancestor origin/main production/main; then
            echo "::error title=Back-sync blocked::staging main is not an ancestor of production/main."
            exit 1
          fi
          git push origin production/main:refs/heads/main
          echo "Fast-forwarded staging/main to production/main."
